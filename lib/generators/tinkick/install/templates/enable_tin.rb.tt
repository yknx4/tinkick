# frozen_string_literal: true

class EnableTinForTinkick < ActiveRecord::Migration[8.0]
  def up
<% extensions.each do |extension| -%>
    enable_extension "<%= extension %>"
<% end -%>
  end

  def down
    raise ActiveRecord::IrreversibleMigration, "TIN may be used by other indexes and optional extensions by other applications; remove them manually if no longer needed."
  end
end
